# YearlyReview 年度回顾组件

类似 Instagram/Spotify 年度回顾的 Stories 风格设计，展示用户一年的咖啡消费数据。

## 目录结构

```
YearlyReview/
├── index.ts                 # 模块导出入口
├── types.ts                 # 类型定义
├── constants.ts             # 常量配置（主题颜色等）
├── hooks.ts                 # 自定义 Hooks
├── YearlyReviewDrawer.tsx   # 主抽屉组件
├── README.md                # 本文档
└── screens/                 # 屏幕组件
    ├── index.ts             # 屏幕导出
    ├── WelcomeScreen.tsx    # 欢迎页
    ├── ScreenContent.tsx    # 屏幕路由
    └── IntroScreen/         # 第一屏：开场动画
        ├── index.tsx
        ├── SegmentTitle.tsx
        ├── SegmentHeadline.tsx
        ├── SegmentImages.tsx
        ├── SegmentReview.tsx
        ├── SegmentWeight.tsx
        └── SegmentGrid.tsx
```

## 屏幕流程

```
┌─────────────────────────────────────────────────────────┐
│  WelcomeScreen                                          │
│  ├─ 标题: Replay'25                                     │
│  ├─ 副标题: 回顾这一年的咖啡                              │
│  └─ 按钮: 前往年度回顾 → 触发 onStart                     │
└─────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────┐
│  IntroScreen (第一屏，包含多个 Segment)                   │
│  ├─ Segment 0: "Replay'25" 标题动画                      │
│  ├─ Segment 1: "这一年你陆续喝了各种咖啡"                  │
│  ├─ Segment 1.5: 咖啡豆图片滑动展示                       │
│  ├─ Segment 2: "一起来回顾下吧"                          │
│  ├─ Segment 3: 今年购买总重量 (大字滚动)                   │
│  └─ Segment 4: 图片网格 + 款数统计                        │
└─────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────┐
│  Screen 2-10 (待开发)                                    │
│  └─ 更多统计数据展示...                                   │
└─────────────────────────────────────────────────────────┘
```

## 动画特性

### 运动模糊效果

使用 SVG `feGaussianBlur` 滤镜实现动态模糊：

- 通过 GSAP ticker 追踪元素速度
- 根据速度动态调整模糊强度
- 快进快出时模糊明显，静止时清晰

### 快-慢-快 动画节奏

每个 Segment 采用统一的动画模式：

1. **快速进入** (0.5s): 从右侧 100% 滑入到 2%
2. **极慢微移** (1.2-1.5s): 从 2% 缓慢移动到 -2%
3. **快速退出** (0.5s): 从 -2% 加速滑出到 -120%

### 颜色过渡

- 每屏使用不同的水果风味色系
- `useColorTransition` Hook 实现平滑的颜色插值
- `GrainGradient` 提供动态渐变背景

## 主题配色

| 屏幕    | 主题        | 主色调  |
| ------- | ----------- | ------- |
| Welcome | 薄荷青绿    | #00B894 |
| 1       | 🍓 莓果     | #FF6B8A |
| 2       | 🍊 柑橘     | #FF9F43 |
| 3       | 🍋 柠檬     | #FFD93D |
| 4       | 🥝 奇异果   | #7CB342 |
| 5       | 🫐 蓝莓     | #6C5CE7 |
| 6       | 🍑 水蜜桃   | #FFAB91 |
| 7       | 🍉 西瓜     | #FF5252 |
| 8       | 🍇 葡萄     | #9C27B0 |
| 9       | 🌴 热带水果 | #FF6F00 |
| 10      | 🍒 樱桃     | #E91E63 |

## 使用方式

```tsx
import YearlyReviewDrawer from './YearlyReview';

<YearlyReviewDrawer isOpen={isOpen} onClose={() => setIsOpen(false)} />;
```

## 数据来源

- **咖啡豆数据**: `useCoffeeBeanStore`
- **用户名**: `Storage.get('brewGuideSettings')` → `settings.username`
- **统计计算**: 根据 `bean.timestamp` 筛选当年数据

## 依赖

- `vaul`: 抽屉组件
- `framer-motion`: 页面切换动画
- `gsap` + `@gsap/react`: Segment 动画
- `@paper-design/shaders-react`: 渐变背景
