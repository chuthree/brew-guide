# YearlyReview 年度回顾组件

类似 Instagram/Spotify 年度回顾的 Stories 风格设计，展示用户一年的咖啡消费数据。

## 目录结构

```
YearlyReview/
├── index.ts                    # 模块导出入口
├── types.ts                    # 类型定义
├── constants.ts                # 常量配置（主题颜色、屏幕时长等）
├── hooks.ts                    # 自定义 Hooks（颜色过渡、运动模糊）
├── YearlyReviewDrawer.tsx      # 主抽屉组件
├── README.md                   # 本文档
└── screens/                    # 屏幕组件
    ├── index.ts                # 屏幕导出
    ├── WelcomeScreen.tsx       # 欢迎页
    ├── ScreenContent.tsx       # 屏幕路由
    ├── EndingScreen.tsx        # 结束页
    ├── IntroScreen/            # Screen 0: 开场动画
    │   ├── index.tsx
    │   ├── SegmentTitle.tsx
    │   ├── SegmentHeadline.tsx
    │   ├── SegmentImages.tsx
    │   ├── SegmentReview.tsx
    │   ├── SegmentWeight.tsx
    │   ├── SegmentGrid.tsx
    │   └── SegmentCost.tsx
    ├── FavoriteRoasterScreen/  # Screen 1: 最喜欢的烘焙商
    │   └── index.tsx
    ├── CategoryFlowScreen/     # Screen 2: 分类流动画
    │   ├── index.tsx
    │   ├── CategoryFlow.tsx
    │   ├── TopBeansList.tsx
    │   └── TopStatsList.tsx
    ├── InsightScreen/          # Screen 3: 综合洞察
    │   ├── index.tsx
    │   └── SegmentExploreStats.tsx
    ├── OriginCategoryScreen/   # Screen 4: 产地分类统计
    │   └── index.tsx
    ├── VarietyCategoryScreen/  # Screen 5: 品种分类统计
    │   └── index.tsx
    ├── ProcessCategoryScreen/  # Screen 6: 处理法分类统计
    │   └── index.tsx
    ├── BrewTimeScreen/         # Screen 7-8: 冲咖啡时间统计
    │   └── index.tsx             # 最早/最晚冲咖啡时间展示
    ├── SummaryScreen/          # Screen 9: 年度总结页
    │   └── index.tsx             # 2025+2x2文案+4张旋转图片
    └── BeanCategoryScreen/     # 咖啡豆分类组件
        ├── index.tsx
        ├── CategoryFlow.tsx
        ├── TopBeansList.tsx
        └── TopStatsList.tsx
```

## 屏幕流程（共 11 个屏幕）

```
┌─────────────────────────────────────────────────────────┐
│  WelcomeScreen                                          │
│  ├─ 标题: Replay'25                                     │
│  ├─ 副标题: 回顾这一年的咖啡                              │
│  └─ 按钮: 前往年度回顾 → 触发 onStart                     │
└─────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────┐
│  Screen 0: IntroScreen (开场动画，约 22.5s)              │
│  ├─ SegmentTitle: "Replay'25" 标题动画                   │
│  ├─ SegmentHeadline: "这一年你陆续喝了各种咖啡"           │
│  ├─ SegmentImages: 咖啡豆图片滑动展示                    │
│  ├─ SegmentReview: "一起来回顾下吧"                      │
│  ├─ SegmentWeight: 今年购买总重量 (大字滚动)              │
│  ├─ SegmentGrid: 图片网格 + 款数统计                     │
│  └─ SegmentCost: 今年咖啡豆总花费 (大字滚动)              │
└─────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────┐
│  Screen 1: FavoriteRoasterScreen (约 5.8s)              │
│  └─ 展示最喜欢的烘焙商统计                               │
└─────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────┐
│  Screen 2: CategoryFlowScreen (约 5s)                   │
│  └─ 图片流动画展示咖啡豆分类                             │
└─────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────┐
│  Screen 3: InsightScreen (约 4.5s)                      │
│  └─ SegmentExploreStats: 探索统计 (产地/品种/处理法数量)  │
└─────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────┐
│  Screen 4: OriginCategoryScreen (约 3s)                 │
│  └─ 产地统计                                             │
└─────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────┐
│  Screen 5: VarietyCategoryScreen (约 3s)                │
│  └─ 品种统计                                             │
└─────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────┐
│  Screen 6: ProcessCategoryScreen (约 3s)                │
│  └─ 处理法统计                                           │
└─────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────┐
│  Screen 7: BrewTimeScreen - earliest (约 4.5s)        │
│  ├─ 图片区域: 2行×5张图片，错落滚入                      │
│  └─ 底部文字: 今年你最早冲咖啡 HH:MM                   │
└─────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────┐
│  Screen 8: BrewTimeScreen - latest (约 4.5s)          │
│  ├─ 图片区域: 2行×5张图片，错落滚入                      │
│  └─ 底部文字: 今年你最晚冲咖啡 HH:MM                   │
└─────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────┐
│  Screen 9: SummaryScreen (约 7.5s)                      │
│  ├─ 顶部: 2025 年份标题                                  │
│  ├─ 中部: 2x2 文案网格                                   │
│  │   ├─ 左上: 最爱烘焙商 (1个)                           │
│  │   ├─ 右上: 最爱产地 (3个)                             │
│  │   ├─ 左下: 最爱豆种 (3个)                             │
│  │   └─ 右下: 最爱处理法 (3个)                           │
│  └─ 底部: 4张图片围圈旋转 (烘焙商圆形，其他正方形)         │
└─────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────┐
│  Screen 10: EndingScreen                                │
│  └─ 结束页，支持重播                                     │
└─────────────────────────────────────────────────────────┘
```

## 动画特性

### 运动模糊效果

使用 SVG `feGaussianBlur` 滤镜实现动态模糊：

- 通过 GSAP ticker 追踪元素速度
- 根据速度动态调整模糊强度
- 快进快出时模糊明显，静止时清晰
- `useMotionBlur` Hook 封装模糊逻辑

### 快-慢-快 动画节奏

每个 Segment 采用统一的动画模式：

1. **快速进入** (0.5s): 从右侧 100% 滑入到 2%
2. **极慢微移** (1.2-1.5s): 从 2% 缓慢移动到 -2%
3. **快速退出** (0.5s): 从 -2% 加速滑出到 -120%

### 颜色过渡

- 每屏使用不同的水果风味色系
- `useColorTransition` Hook 实现平滑的颜色插值（easeOutCubic 缓动）
- `GrainGradient` 提供动态渐变背景

### 屏幕时长配置

```typescript
// SCREEN_DURATIONS 数组定义每个屏幕的动画时长（秒）
[22.5, 5.8, 5, 4.5, 3, 3, 3, 4.5, 4.5, 18]; // 对应 Screen 0-9
```

## 主题配色

| 屏幕    | 主题      | 主色调  |
| ------- | --------- | ------- |
| Welcome | 薄荷青绿  | #00B894 |
| 0       | 🍓 莓果   | #FF6B8A |
| 1       | 🍊 柑橘   | #FF9F43 |
| 2       | 🍋 柠檬   | #FFD93D |
| 3       | 🥝 奇异果 | #7CB342 |
| 4       | 🫐 蓝莓   | #6C5CE7 |
| 5       | 🍑 水蜜桃 | #FFAB91 |
| 6       | 🍉 西瓜   | #FF5252 |
| 7       | 🌅 日出   | #FF8C42 |
| 8       | 🌙 星夜   | #3D5A80 |
| 9       | 🦶 高级灰 | #4a4a4a |
| 10      | 🍒 樱桃   | #E91E63 |
| Ending  | 宁静蓝    | #0984E3 |

## 类型定义

```typescript
// 颜色元组类型
type ColorTuple = [string, string, string, string];

// 屏幕主题配置
interface ScreenTheme {
  colors: ColorTuple;
  accent: string;
}

// 屏幕组件通用 Props
interface ScreenProps {
  onComplete?: () => void;
}

// 带咖啡豆数据的屏幕 Props
interface BeanScreenProps extends ScreenProps {
  beans: CoffeeBean[];
  beanImages: string[];
  totalWeight: number;
}

// Segment 通用 Props
interface SegmentProps {
  onComplete?: () => void;
  blurRef?: React.RefObject<SVGFEGaussianBlurElement | null>;
}
```

## 使用方式

```tsx
import YearlyReviewDrawer from './YearlyReview';

<YearlyReviewDrawer isOpen={isOpen} onClose={() => setIsOpen(false)} />;
```

## 数据来源

- **咖啡豆数据**: `useCoffeeBeanStore`
- **用户名**: `Storage.get('brewGuideSettings')` → `settings.username`
- **统计计算**: 根据 `bean.timestamp` 筛选当年数据
- **图片提取**: 从咖啡豆列表中提取有图片的条目（最少 5 张，不足时重复填充）

## 依赖

- `vaul`: 抽屉组件
- `framer-motion`: 页面切换动画
- `gsap` + `@gsap/react`: Segment 动画
- `@paper-design/shaders-react`: 渐变背景
- `lucide-react`: 图标组件

## 集成功能

- **历史栈管理**: `useModalHistory` 支持返回键关闭抽屉
- **主题颜色同步**: `useThemeColor` 同步顶部安全区颜色
- **进度条**: 顶部显示屏幕进度，支持手动切换屏幕
