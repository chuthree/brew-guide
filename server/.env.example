# ==================== 必需配置 ====================

# API 密钥 (必填)
# 阿里云百炼平台 API Key (用于年度报告、反馈审核)
SILICONFLOW_API_KEY=your_dashscope_api_key_here

# 七牛云 API Key (用于咖啡豆识别)
QINIU_API_KEY=your_qiniu_api_key_here

# ==================== 服务器配置 ====================

# 服务端口 (可选，默认 3100)
PORT=3100

# 运行环境 (可选，默认 development)
NODE_ENV=development

# 日志级别 (可选，默认 info)
# 选项: error, warn, info, debug
LOG_LEVEL=info

# ==================== 安全配置 ====================

# 管理员密钥 (可选，默认 brew-guide-admin-2025)
ADMIN_KEY=brew-guide-admin-2025

# IP 哈希盐值 (可选，默认 brew-guide-salt-2025-secure)
IP_HASH_SALT=brew-guide-salt-2025-secure

# ==================== CORS 配置 ====================

# 允许的前端域名 (可选，逗号分隔)
# 不设置则使用默认值: http://localhost:3000,http://localhost:3001
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001,https://yourdomain.com
